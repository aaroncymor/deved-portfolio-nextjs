import { motion } from "framer-motion";

const IntroductionAnimation = () => {
  return (
    <>
      <motion.h1
        className="fixed w-[10ch] z-[2] text-white text-6xl top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 animate-blinking-caret animate-typing-intro-1 overflow-hidden border-r-2 border-r-neon-green border-solid whitespace-nowrap mx-auto my-0"
        initial={{ opacity: 1 }}
        animate={{ opacity: 0 }}
        transition={{
          delay: 3,
        }}
      >
        Hi! I am Aa.
      </motion.h1>

      <motion.h1
        className="fixed w-[20ch] z-[2] text-white text-6xl top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 animate-typing-intro-2 overflow-hidden border-r-2 border-r-neon-green border-solid whitespace-nowrap mx-auto my-0"
        initial={{ opacity: 1 }}
        animate={{ opacity: 0 }}
        transition={{
          delay: 10
        }}
      >
        I'm a Fullstack Developer.
      </motion.h1>
      <motion.div
        className="fixed z-[1] bg-gray-900 h-full w-1/2 top-0 overflow-hidden left-0"
        initial={{ left: '0' }}
        animate={{ left: '-50%' }}
        transition={{
          type: "spring",
          delay: 3,
          duration: 2.5,
        }}
      />
      <motion.div
        className="fixed z-[1] bg-gray-900 h-full w-1/2 top-0 overflow-hidden right-0"
        initial={{ right: '0' }}
        animate={{ right: '-50%' }}
        transition={{
          type: "spring",
          delay: 3,
          duration: 2.5,
        }}
      />
    </>
  );
}

export default IntroductionAnimation;
