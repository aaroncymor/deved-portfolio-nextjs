import Link from 'next/link';

const Footer = ({children}) => {
  return (
    <footer>
      <nav className="nav__footer">
        <ul>

        </ul>
      </nav>
    </footer>
  );
}

export default Footer;
